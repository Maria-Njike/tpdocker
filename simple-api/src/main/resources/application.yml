server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://db-container:5432/db
    username: usr
    password: pwd
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
