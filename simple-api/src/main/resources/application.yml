server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://postgrescontainer:5432/mydatabase
    username: usr
    password: pwd
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
